# PR 158 全量审计 · 2026-09-12

审查对象：[#158](https://github.com/iroha1145/option-pro/pull/158) `fix: 修复审计确认的数据与恢复流程问题`  
初审 Head：`70f06e0ba053ba9dae88596b40e427132fd73b4d`  
复测 Head：`c5c62be04bc1f48eeac88aa09f71dd2e0d7d72ac`（`codex/audit-fixes-f790b7b`）  
Base：`f790b7bfb7e460484d11e1b3eec75df14a318d74`（`main`）  
提交：`6e0b067` → `70f06e0` → `b62ab55` → `bc6dae` → `2804d49` → `190eede` → `06c8bbf` → `75077c0` → `7530c3c` → `c5c62be`  
对照说明：[AUDIT_FIXES_2026-09-12.md](https://github.com/iroha1145/option-pro/blob/codex/audit-fixes-f790b7b/docs/AUDIT_FIXES_2026-09-12.md)

本文件是独立全量审计的书面记录。审查只提交意见，不改业务代码。结论以当前工作树、命令输出和可复现运行时为准，不以 PR 说明或测试套件“全绿”代替逐项核验。

线上意见入口：

- 初审总评：https://github.com/iroha1145/option-pro/pull/158#issuecomment-5645271084
- 复测总评（`b62ab55`）：https://github.com/iroha1145/option-pro/pull/158#issuecomment-5645424703
- CI 复看（`bc6dae`）：https://github.com/iroha1145/option-pro/pull/158#issuecomment-5645554322
- CI 复看（`190eede`）：https://github.com/iroha1145/option-pro/pull/158#issuecomment-5645858700
- 行评：`market.py:117`、`useAccess.tsx:121`、`Watchlist.tsx:378` / `569` / `602`、`Layout.tsx:32`、`earnings.py:1279`
- 对 Codex 原评的回复：确认 P1（指数空涨跌）在 `70f06e0` 成立；`b62ab55` 已修。不采纳 16:00 ET bar 标盘后作为门禁

## 结论

**`c5c62be` 复测：功能门禁与 CI 门禁均已闭合，本轮未发现新的 P1/P2，可以合并。**

初审（`70f06e0`）的 P1-A / P1-B / P2 在 `b62ab55` 已修，后续提交补齐了静态断言、首次身份门、凭据切换、身份限流等待，以及绘图同步恢复的撤销保护。GitHub Actions 两轮 `test` 在 `c5c62be` 上均为 success。独立复测未推翻既有修复，也未在新绘图路径上发现会误删外部图形或回滚已确认版本号的问题。

历史审查原文保留在后文，作为对照。

## 审查范围与方法

覆盖前后端源码、公开快照契约、自动化、浏览器回归、Mock/Live UI 走查，以及身份失败独立复现。

| 入口 | 结果 | 说明 |
|---|---|---|
| GitHub Actions `test` | 两轮 success | [34684703148](https://github.com/iroha1145/option-pro/actions/runs/34684703148)、[34684705461](https://github.com/iroha1145/option-pro/actions/runs/34684705461) |
| 后端全量 pytest | **3458 passed, 6 skipped** | `PYTHONPATH=backend`，约 301 秒 |
| 审计相关后端子集 | 113 passed | 含 `test_audit_*`、行情语义、财报来源 |
| 前端 Node | 904 / 905 | 唯一失败见下方「环境脏 dist」，不是 PR 源码回归 |
| ESLint | 0 error，2 warning | 警告在未改文件 `FeedPanel.tsx`，与 PR 说明一致 |
| `npm --prefix frontend-src run test:audit-fixes` | **23 / 23 passed** | 选股恢复、触控、身份缓存、图表 ET |
| 演示财报契约 Playwright | passed | `mock-earnings-contract.spec.mjs` |
| Live Vite 身份 503 复现 | **列表被清空** | alice 已登录且可见 AAPL，focus 后 `/access/status` 变 503 |
| Mock UI 走查 | 未见版面级破损 | 首页、自选、选股、详情、财报、登录、雷达、板块、大盘；约 390px 窄屏 |

未声称：生产账户、真实供应商准确性、正式双容器部署、或线上已修复。

## 必须修复

### P1-A 指数 `change_percent: null` 三角契约断裂

**声称（B-LOG-6）：** 指数缺少有效昨收时保留最新价，将涨跌幅留空。

**生产者已做到。** `backend/app/api/market.py` 在缺昨收或昨收非有限时返回 `None`，不再用 0 冒充平盘：

```python
"change_percent": round((price - prev) / prev * 100, 2) if prev is not None else None,
```

**消费者未对齐，本 PR 也没有改这两处：**

| 消费者 | 位置 | 实际行为 |
|---|---|---|
| 公开首页快照 | `backend/app/public_home_snapshot.py` `_validate_indices` 约 500 行 | 有价时要求 `_finite_number(change)`。`change_percent is None` 使整包 `create_public_home_entry('indices')` 抛 `ValueError: invalid public home payload: indices`。访客首页 503 或继续吃旧快照。 |
| 前端映射 | `frontend-src/src/api/modules/market.ts` `mapIndices` 约 42 行 | `price === null \|\| changePct === null` 丢行。`IndexTape` / 首页指数卡永远看不到「有价无涨跌」。 |

`public_home_snapshot.py` 在本 PR 只补了财报 `estimate_sources`，没有改 `_validate_indices`。个股 `_validate_overview` 已允许 optional change，指数没有。

独立复现（审查环境）：

- `_validate_indices(有价 + change_percent=null)` → `False`
- `create_public_home_entry('indices', mixed)` → `ValueError: invalid public home payload: indices`
- `mapIndices` 丢弃 `change_percent` 为 `null` 的行

`70f06e0` 未修 Codex 对 `6e0b067` 的同一条 P1。

**修复方向：** 与个股概览同一契约。有价无涨跌视为合法；前端渲染价格并显示涨跌未知，不要丢行，不要让 worker 整包失败。

### P1-B N2 不完整：短暂身份 503 清空已登录自选

**声称（N2）：** 同身份核验及暂时无法读取身份时保留页面。

**页面实例保留，列表不保留。** `Layout.tsx` 的 `pageKey` 只用 `pathname` / `role` / `username`，不含 `identityUnavailable`，因此不 remount，顶栏仍显示「退出 alice」。真正清数据的是身份失败后的状态机。

链路：

1. `frontend-src/src/hooks/useAccess.tsx` 约 121 行：失败时 `setQueryPrincipal(null)` + `setIdentityUnavailable(true)`。注释写「保留当前已知身份」，实现把查询主体置空。
2. `frontend-src/src/hooks/usePersonalWatchlist.ts`（本 PR 未改）约 15、83 行：`identityUnavailable` 时 `enabled = false`，不再暴露已缓存 `tickers`；`error` 合成为「身份暂时无法确认，请稍后重试」。
3. `frontend-src/src/pages/Watchlist.tsx` 约 378–386、804–847 行：`enabled: !personal.error` 停行情；`personal.error` 时 `items = []`；`personalFailed` 整页换成 EmptyState「自选读取失败」。

行情失败路径已经区分「还有上一轮数据时不整块换错误页」；名单/身份失败没有同等保护。

独立 Playwright（live Vite `127.0.0.1:3148`，`VITE_API_MODE=live`，alice 已登录且名单含 AAPL；focus 后拦截 `/access/status` 为 503）：

| 指标 | 失败前 | 失败后 |
|---|---|---|
| `[data-quote-symbol="AAPL"]` | 1 | 0 |
| 「自选读取失败」 | 0 | 1 |
| 「身份暂时无法确认，请稍后重试」 | 0 | 1 |
| 「退出 alice」 | 1 | 1 |

主区同时出现「0 只标的 / 上限 50」「暂时读不到你的自选列表，请重试。」「自选读取失败」。顶栏身份未变。

现有 E2E（`audit-principal-scope`）只断言选股草稿在身份扰动后仍在，**没有**断言已登录自选列表仍在。催化 `frontend-src/src/components/catalysts/useCatalystResource.ts` 约 20、44 行在 `identityUnavailable` 时返回空快照 + 503，同类风险。

**修复方向：** 短暂身份失败应保留上一份已确认名单与行情，只加横幅和重试；不要把合成的 `personal.error` 当成「名单不存在」。`queryPrincipal` 在短暂失败时不应清空。应补浏览器断言：已登录自选在 `/access/status` 503 后列表仍在。

## 建议同批修复

### P2 同日不同 fiscal period 仍标 `confirmed`

`backend/app/api/earnings.py` 约 1270–1279 行：日期相同就写 `calendar_date_status = "confirmed"`。`_same_earnings_report` 只挡住 `eps_estimate` / `revenue_estimate` 合并。

同日但财政年度或季度不同时，日历元数据仍像双源确认。前端暂不渲染该字段，影响在快照和 API。`confirmed` 应与 estimate 合并使用同一期次谓词；日期相同但期次冲突应记 conflict 或单独状态。

## 已核验成立、不作为本轮门禁

以下与 PR 说明和自动化一致，走查未发现反向证据。

- 财报日期或已知年季冲突时不从另一报告补预测；`estimate_sources` 逐字段记录，兼容旧快照。
- 个股概览：日聚合不冒充最新价；Massive 分钟价缺昨收时留空涨跌，不跨供应商拼昨收。
- 未知成交量在分析层保持缺失，真实零成交保持零；图表指纹区分未知与零。
- 自选共享交易时段，半日市及正式收盘成交与实时行情一致。
- 选股错误区重试保留条件与草稿；重置含宏观条件。
- 表格恢复原生行/单元格；真正的按钮负责键盘展开。
- 雷达换股按代码重建闪色，不把上一只的涨跌闪到下一只。
- 分钟图、提示框、指标时间统一美东时间；日线/周线按交易日。
- 手机搜索、登录、退出、密码显示及分页触控约 44px；320/390、中英日布局通过审计浏览器套件。
- 明确会话失效后清用户名并暂停自选写入；身份未确认窗口不写持久缓存。
- 演示财报日历每家公司一条，覆盖历史、近期与跨月。

### 明确不采纳为逻辑缺陷

**Codex：16:00 ET 的 5m bar 应标盘后。** 与仓库设计一致。`market_session` 把整点收盘印当 `regular`；`tests/test_api_data_states.py` 已按 `regular` 断言。若改成盘后，会和半日市/正式收盘成交语义以及本 PR 的 B-LOG-7 测试冲突。若产品要区分「16:00 整点 bar」与「收盘印」，应先改会话函数的单一口径和测试，而不是只在自选聚合路径特判。

**SideCards「已应用其他筛选」。** 与 `macroFilteredBase` 一致，不是文案错误。

**桌面 ResultTable 分页 `size-7`。** 移动端走 44px `PagerButton`，审计浏览器已覆盖 320/390。

**本地 Node `build.test.mjs` 失败。** 本机残留 `frontend-src/dist/index.html` 引用 `index-BeOEaab3.js`，提交产物为 `index-DShXX0S1.js`。环境脏 dist，不是 PR 源码回归。CI 已 `diff -r frontend-src/dist frontend`。

## 覆盖缺口（非本轮门禁）

- 图表 API `backend/app/api/stocks.py` 约 3746 行把非有限 volume 收成 `0`。分析层已区分未知/零，该行不在本 PR diff，B-LOG-5 未完全闭环。
- 自选 Massive 路径仍 `stamp/1000`，未走 `snapshot_minute_quote` 的多单位解析。
- `chartTime` 拒无时区 ISO。live `t` 是 epoch，经 `toISOString()` 带 Z，当前不是实害。
- 催化 TTL / 45s 静默换行更像有意新鲜度，未当缺陷。
- 审计浏览器套件没有「已登录自选在身份 503 后仍显示上一份名单」；P1-B 因此能在 23/23 全绿下漏过。

## 身份失败复现要点

隔离条件：只放行本机；`/api/access/status` 先返回已登录 alice，focus 后改为 503；`/api/account/watchlist` 固定 `["AAPL"]`；`/api/stocks/watchlist` 返回带价的 AAPL 行。

期望（按 N2 原文）：顶栏身份保留，AAPL 行仍在，可出现「身份暂时无法确认」横幅。  
实际：顶栏身份保留，AAPL 行消失，主区整页「自选读取失败」，计数变成 0 只。

## 合并门槛

在以下两项闭合前不应合并：

1. **P1-A：** `_validate_indices` 与 `mapIndices` 接受「有价 + 涨跌空」；有回归测试证明 worker 能发布该形状，前端能显示价格且不把涨跌编成 0。
2. **P1-B：** 已登录用户在 `/access/status` 短暂 503 后仍看到上一份自选名单和行情；浏览器测试覆盖该路径。催化空快照是否同步处理，建议一并声明。

P2 建议同批把 `calendar_date_status: "confirmed"` 与 `_same_earnings_report` 对齐。

本文件不替代修复。修复仍应落在 #158 或后续合并请求，并更新 [AUDIT_FIXES_2026-09-12.md](./AUDIT_FIXES_2026-09-12.md) 中已不再成立的「已修复」表述。

## 复测 · `b62ab55`（2026-09-12 后续）

线上复测总评：https://github.com/iroha1145/option-pro/pull/158#issuecomment-5645424703

### 门禁对照

| 初审项 | 复测 | 证据 |
|---|---|---|
| P1-A 指数有价无涨跌 | 已修 | `_validate_indices` 允许 `change is None`；`create_public_home_entry('indices')` 可发布。独立脚本：`validate_mixed True`，价 `6123.45`、涨跌 `None`；`NaN` 涨跌仍拒绝。`mapIndices` 保价且 `changePct=null`。浏览器纸带 `SPX 6,123.45 —`，aria「涨跌数据缺失」，卡片 `—`，不出现 `0.00%`。 |
| P1-B 身份 503 清空自选 | 已修 | `usePersonalWatchlist` 保留同 key 内存快照；`personalFailed` 仅当 `myTickers === null`。独立复现：`apple 1→1`，`failTitle 0`，横幅「身份暂时无法确认」，「管理自选」禁用，计数仍为 1 只。催化 `confirmed` 快照同样保留。 |
| P2 同日期次 `confirmed` | 已修 | `same_report` 同时决定确认与 estimate 合并；同日不同年季写 `conflict` 且带 `{earnings_date, quarter, year}`。`test_conflicting_report_never_supplies_missing_estimates` 通过。 |

未采纳「短暂失败仍保留 `queryPrincipal`」：与修复说明一致。可见内容走同身份内存快照，持久缓存仍禁止。

### 复测入口

| 入口 | 结果 |
|---|---|
| 后端全量 pytest | **3488 passed, 6 skipped** |
| 定向后端（指数/财报/缺口） | 149 passed |
| `npm --prefix frontend-src run test:audit-fixes` | **33 / 33 passed** |
| ESLint | 0 error / 0 warning |
| 前端 Node（与 CI 同命令） | CI **906 / 908**；本地另多 1 条脏 `dist`，可忽略 |
| GitHub Actions `test` | 两轮 **failure**：`34689033188`、`34689031554` |

### 仍阻塞：静态断言未随实现更新

行为正确，但源码扫描测试未改，CI「Run frontend behavior tests」失败：

1. `frontend-src/tests/audit-findings-fix.test.mjs` 仍要求 `/disabled=\{personal\.busy\}/`。实现已是 `disabled={personal.busy || !personal.enabled}`（`Watchlist.tsx` 约 569 行）。
2. `frontend-src/tests/perf-list-and-requests.test.mjs` 仍要求 `useQuoteSymbols(cardItems.map(`。实现已是 `useQuoteSymbols(identityUnavailable ? [] : cardItems.map(`（约 602 行）。身份可用时订阅对象仍是完整 `cardItems`。

请改断言，不要改回旧实现。

### 复测后的合并门槛

1. 更新上述两处静态断言。
2. CI `test` 转绿。

初审 P1/P2 不再作为功能门禁。B-LOG-5 图表缺量、`minute.t/1000` 等初审「非门禁」项，修复说明已分别补齐或说明不采纳，本轮未推翻。

## 复测 · `c5c62be`（2026-09-12 再审）

对照 `190eede` 之后的 4 个提交：`06c8bbf`（真实身份限流等待）、`75077c0`（同图表同步恢复保留撤销）、`7530c3c`（外部绘图变更作废旧撤销）、`c5c62be`（选股标签共用身份就绪入口）。

### 上次遗留

| 项 | 结果 | 证据 |
|---|---|---|
| P1-A 指数有价无涨跌 | **仍成立** | `_validate_indices` 接受全员/混合 `change_percent is None`；零价仍拒绝。独立脚本：`validate_all_price_null_change True`，`validate_mixed_null_and_finite True`，`validate_zero_price_rejected False`。`mapIndices` 保价且 `changePct=null`。浏览器：纸带 `SPX 6,123.45 —` + aria「涨跌数据缺失」；首页卡价格 `6,123.45`、涨跌 `—`；IXIC 真实平盘仍显示 `0.00%`。 |
| P1-B 身份 503 清空自选 | **仍成立** | `usePersonalWatchlist` 在 `identityUnavailable` 时保留同 key 内存快照；`items` 仅当 `myTickers === null` 才清空。独立复现：alice + AAPL 在 focus 后 `/access/status` 503，名单、价格、`1 只标的` 仍在，「管理自选」禁用，出现「身份暂时无法确认」。`audit-principal-scope` 第 10 项通过。 |
| P2 同日期次 `confirmed` | **仍成立** | `_same_earnings_report` 同日不同季度为 `False`，同期为 `True`。`test_conflicting_report_never_supplies_missing_estimates` 仍通过。 |
| `190eede` 首页视觉超时 | **已修** | `appearance.spec.mjs` / `catalyst-desk.spec.mjs` 对首页正文改为最多等 75s；`tabs-palette-glide.spec.mjs` 的选股五项共用 `openScreener()`，等 `[aria-label="筛选条件"]` 最多 75s。身份错误本身不算页面验收通过。 |
| 静态断言 | **已修** | `bc6dae` 已对齐 `disabled={personal.busy \|\| !personal.enabled}` 与 `useQuoteSymbols(identityUnavailable ? [] : …)`。 |

### 新改动审查（绘图同步恢复）

`useDrawingController.loadScope` 在同 scope 重试成功时：业务内容未变则 `historyReplace` 保留撤销/重做；外部新增、删除或改价则 `createHistory` 作废旧快照。失败且缓存缺失时不把空缓存当成远端删除。迟到 GET 同时核对 `scopeLoadId`、`stateVersion` 和 `scopeRevision`，被编辑作废的 429 仍遵守 `Retry-After`。

15 项 `drawing-sync-recovery.test.mjs` 全部通过，覆盖：锁后限流恢复仍可撤销、迟到 GET 不盖新编辑、切股票/账号/「用服务器版本」仍重置、外部增删改价后旧撤销不发 DELETE/POST/PUT、仅 revision/时间戳变化保留撤销。

未发现新的功能门禁。采用外部新增时会丢掉本地未完成的撤销栈，这是为了避免撤销误删另一页的图形，与修复说明一致。

### 本轮入口

| 入口 | 结果 |
|---|---|
| GitHub Actions `test` | 两轮 **success**：[34698136069](https://github.com/iroha1145/option-pro/actions/runs/34698136069)、[34698134115](https://github.com/iroha1145/option-pro/actions/runs/34698134115) |
| 后端全量 pytest | **3488 passed, 6 skipped**（合计 3494，与 CI 一致） |
| 定向后端（指数/财报/公开快照） | 128 passed |
| 前端 Node 全量 | 926 / 927；唯一失败仍是本机脏 `dist`（`index-BeOEaab3.js` vs 提交产物 `index-Bnz11sk4.js`），与 CI 无关 |
| 前端 Node 定向（绘图/身份/指数/自选） | 71 passed |
| `npm --prefix frontend-src run test:audit-fixes` | **38 / 38 passed** |
| 独立浏览器取证 | 指数未知涨跌、自选 503 保名单均通过 |

### 非门禁观察

- `/catalysts`、`/breakouts` 的 Catalyst Desk 取证仍只验导航壳，不等 `Outlet`。身份确认慢时，截图可能拍到加载态；不影响产品正确性，也不挡合并。
- 本机截图环境缺中文字体，字形会糊，断言与 CI 不受影响。

### 复测后的合并门槛

无。功能门禁、静态断言和身份就绪等待均已闭合。后续若再改身份门或绘图恢复，应保持「等身份就绪再找页面正文」以及「外部变更不作废为误删」这两条。
