# PR #148 复审 R1 / R2 修复

审查起点：`776e2fa33e70bf60017cb72edd95291f24c99192`。正式回归先在该提交上失败，再在本修复提交上通过。测试直接导入生产函数，没有复制审查包摘录。

## R1：更晚保存时间不能覆盖更早行情

原子发布前比较同一口径的输入截止时间（`extract_score_data_through` 取全池最早日期）、评分版本和带日期的可用行数。较早输入、未知输入时间或同日覆盖度下降时返回 `kept_previous_snapshot`，并在任务结果里写下具体 `reason`。迟到 `saved_at` 仍是 `kept_newer_publish`。同交易日重算且覆盖度未下降可以写盘。

## R2：变体降级不再被当成整轮成功

定时 `__call__` 检查每个变体的 `TaskResult`。尝试数、真实发布数、保留旧数据数、失败数分开计数；失败条目含参数哈希、错误码和原因。返回降级与抛异常都算失败。默认成功而变体失败时状态为 `degraded`、`error_code=strength_variant_refresh_failed`，有界重试 300 秒且不超过默认定时剩余时间；重试不再重跑已成功的默认快照，也不推迟默认截止。仅默认失败时仍使用原来的 `strength_input_unavailable` 语义。

## 回归

`tests/test_strength_publish_guard.py` 覆盖写入层、`StrengthRefreshTask._run`、部分落后、未知时间、同日重发、变体降级/异常/部分失败/全成功，以及重试截止时间。原有迟到写入与取消后不得发布的测试保留。

正式回归先在审查提交 `776e2fa3` 上失败（10 项失败、3 项通过），再在修复提交上通过。

## 完整 CI

最终候选提交：`68740a31b0e6293a98fdad931b065ac780c67f2b`。

- 推送检查：https://github.com/iroha1145/option-pro/actions/runs/34185808856 （success，任务 `101933781217`）
- 合并请求检查：https://github.com/iroha1145/option-pro/actions/runs/34185810827 （success，任务 `101933787129`）
- 后端：`python -m pytest -q` → `3325 passed, 5 warnings`，退出码 0
- 前端行为与翻译：`# tests 822` / `# pass 822` / `# fail 0`
- 浏览器：页面/图表 72 + 10、报价/自选 21、恢复 6、选股 19、原生缓存 1、全站取证 64，合计 193
- 生产构建、提交产物一致性、镜像、容器、数据库及只读根目录步骤均为 success

合并请求保持草稿；不合并，不部署。
