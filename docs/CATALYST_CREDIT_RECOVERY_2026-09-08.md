# 新闻催化剂充值后恢复修复

生产记录确认：2026-09-08 07:42:57 UTC 后已没有新的欠费失败，08:48:12 UTC 的新闻分析成功完成，但分析可用性仍返回 `provider_credit_exhausted`，手动市场焦点请求被拦截。

旧逻辑把两小时内任何一次欠费记录当成当前余额状态，没有检查后续是否已有成功分析。手动入口又把这一历史信号当成禁止重试的条件，返回通用的“当前无法开始新闻分析”。

修复后，后续成功且确实提交过的分析会清除旧欠费信号；新的欠费失败仍会重新显示提示。充值后的管理员手动请求允许进入原有队列，每日用量、冷却、权限和工作进程检查保持有效。页面提示改为“上次分析余额不足，充值后可重试”，焦点周期提交与结束后同步刷新可用性及历史对照。

验证包括失败、充值后成功、再次欠费、仅有待执行任务和旧记录过期的状态变化，以及手动新闻和焦点周期接口。原逻辑下七项定向检查失败，修复后通过。相关后端回归 916 项通过；前端行为检查 28 项通过；正式构建及产物一致性通过。代码规范检查无错误，保留两项既有警告。

浏览器使用本地模拟接口验证手动提交只发送一次请求，新周期完成后自动显示新结果，并立即更新顶部可用性。生产恢复情况单独记录在发布验证材料中。
