# PR #162 审查修复

修复基准：`d62054e0a4820a7fe51f6a4e12cd9a164cb0485c`。本轮处理四项 P2 与一项 P3，未重新测量性能，也不将已有实验室结果视为生产验证。

## 修复内容

- 新闻抽屉的初次加载、手动重试和任务完成后的详情刷新共用请求顺序与取消机制。旧响应不能覆盖已完成结果，关闭、换新闻和较新读取会作废旧请求及其重试等待；新创建的任务同样保护自己的状态。
- 同一主体的后台身份核验沿用已确认的分析能力，保留财报分析和生成确认框。首次加载仍可提前显示页面；真实能力变化、主体变化、凭据写入和明确会话失效仍更新或清理权限。
- 延迟开启行情流保留已安排的重试时间，并等待正在进行的权限探测或行情快照，不提前重新请求。
- 最终回归脚本遇到生产静态产物差异或比较错误时保留日志并失败退出。
- 交错测量报告补充超时数量、失败率和比较完成状态。成功样本的原有分位数继续保留；不完整的冷/热比较不计算差值，整个测量保存报告后以失败退出。

## 回归覆盖

新增用例覆盖详情响应乱序、旧详情错误、任务替换、关闭重开、取消退避、正常身份核验保留内容、真实权限关闭与旧响应拦截、行情重试期限与在途请求、产物差异及读取错误，以及全部成功、部分超时、全部超时的统计结果。

- 前端行为测试：982/982 通过，0 跳过；其中新增 24 项回归。
- 浏览器身份与数据恢复：22 项通过。
- 新闻抽屉响应乱序、财报确认框保留：真实 React 与隔离 Chrome 复核通过。外围显示组件及接口使用受控替身，保留真实状态逻辑。
- 静态检查、正式模式构建、生产静态断言、产物逐文件一致性及脚本语法检查通过。
- 用现有 r5 八组交错原始记录复算，四个 p75 与两个差值完全一致，新增超时计数均为 0。旧样本保持原样。

本轮没有后端修改；浏览器验证使用本地模拟数据，不代表生产环境、真机或容量压测。
