# CI run 35444810333（未删断言，未把“疑似偶发”写成根因）

- 事件：`pull_request` job `test` 失败。
- 合并 SHA（CI 环境 `APP_COMMIT`）：`e1461839d60c14699323fbb51f7ebecc608ada16`
- 分支 head 同日 push `3de4bdf5`：同一 workflow **通过**。
- 失败步骤：`Capture Catalyst Desk and macro conditions visual evidence`
- 失败用例：`visual-tests/tabs-palette-glide.spec.mjs:119` — `command palette glide highlight › highlight is visible on first open...`
- 错误：`TimeoutError` 15s，等待 `getByRole('heading', { level: 1 }).first()`
- Playwright 页快照（error-context.md）：主导航已在，主区只有状态 **“身份暂时无法确认，请稍后重试”**，没有 h1。
- 工件：`test-failed-1.png`、`trace.zip`，已下载到 `/opt/cursor/artifacts/ci-35444810333/catalyst-desk-visual-evidence-e1461839d60c14699323fbb51f7ebecc608ada16/playwright/tabs-palette-glide-command-80435-animating-from-a-stale-spot/`
- 本机当前 `/` 有 title、无该中文身份文案、也无 h1。这是另一次渲染，不能用来否定 CI 页快照。
- 未删除断言，未放宽超时，未把根因写成“偶发动画”。记录到的事实是：该次 PR 合并检出上，首页身份确认失败导致 h1 未出现。
