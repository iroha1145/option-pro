# /goal PR #174：在既有特征带上完成可信的 Round 1b 比较与主题结论

审阅锚点 `70a57ce5b60acfedb7ac4337b13aa2ee3a627d5f`；先读最新 PR head，识别后续已修复项，不覆盖新改动。阅读本包 REVIEW.md、evidence/probes.json 和 tests/test_review_contracts.py。测试是源码隔离反例，需要在真实仓库加等价集成断言；不要用通过数代替语义检查。

本轮交付是一次有限的算法实验重评，不是再开底层重构。保留已完成的行情、B0 特征、账本、EOD、日期对齐、平均秩、主题/参考池分离、生产边界。Cursor 没有 Massive；复用原合法 Yahoo/本地缓存，不采购，不重新全量抓价。所有24主题一对一输出；美股完整EOD，研究离线；生产默认、实时详情、账户、期权、日股不动；2024-07-01 起留出不打开；已经看过的开发区不重新称为未见样本外。不自动合并/晋升。

## A. 真实入口的实验隔离（先于评分）

1. 验证冻结 B0 特征文件的真实内容 SHA256，核对既有 expected hash `b490ba6d83965a0c3fe60c76afab8205fc2a0d76cc6577cdf23cb838e447f75a`。一致才登记使用；不一致停止该运行并报告来源，不悄悄更新 expected hash。不得使用字面量 `b0_measurement_factor_rows` 充当内容哈希。
2. run_id 至少包含：输入内容/成员/参考池哈希或可验证版本、完整配置、能力掩码、变体完整定义、档位/评分期限/收益期限、研究范围、可用时点/标签政策、事件规则、配对/统计/块长度/seed定义及相关计算代码哈希。URL记录或无关CI元数据可单独追踪，不让它们强制重算。
3. `--limit`、合成测试与正式全带运行独立目录。主入口实际使用内容绑定的目录/manifest，非只测试 runs.py helper。变评分则重评分；权重无关特征允许复用。报告区分原特征提取、重评分、汇总以及实际重试，不硬编码零重算。
4. 旧 B0 和 Round1 结果不覆盖。R1b 标明被替代字段和原因。验证变数据、变一个权重、变变体定义、变标签/统计规则均拒绝旧checkpoint或新建运行；同签名整跑/分块/恢复一致；文件写完但checkpoint未写不重计，重复证券主键报错/可审计幂等。

## B. 统一能力基线后，才做因子消融

1. `g_is_available` 必须区分 actual_G_observed 和 can_score_without_G。缺 G 容忍评分不等于 G 可用。本次数据中无真实 G，则不运行名为“G有/无”的经济消融；82个现有伪 G 消融保留旧运行记录、标 capability-confounded。
2. FULL_EIGHT/B0 不改覆盖门槛、不填假行业/中性G、不因为无法评分淘汰算法。
3. 对全部24主题构建固定 PRICE_ONLY_DIAGNOSTIC 基线；D单独命名 D_MARKET_RESIDUAL_DIAGNOSTIC。可用集合与必需字段预登记，固定排除不可观测G后归一化，然后在各自轨道对 T/M/S/B/P/V/R 逐项评分消融。不要只有价格轨基线、所有变体仍挂FULL_EIGHT。
4. D 的两个同值别名不能算两次独立证据；无行业数据不能声称检验了双因子行业残差。
5. 这是移除评分贡献，不移除结构/风控硬否决；缺失字段仍是缺失，不能补分通过。不同能力轨不直接选冠军。任何覆盖变化单独报，不归因于排序改善。

## C. 共同成员配对与时间相关性

1. 每组保存 `(session, theme, family, track, profile, score_horizon, label_horizon, security_id)`。baseline与variant按ID内联，取共同可评分且标签成熟/有限集合；对共同集合中的两侧分数分别重排平均秩，再计算IC差。
2. 别把各自IC相减叫配对。各自覆盖、共同样本数、新增/丢失名单和缺失原因分列。跨主题不合并成更多独立股票；重复ID不提高n。单日n<10或常数向量保留null原因，不跨日凑n。零配对组要有明确预期网格，而不是直接消失。
3. 在计算任何新结果前登记不确定性方法。例如 circular/stationary date-block bootstrap，seed174、2000重复，H和2H为固定敏感性长度（5/20/63分别使用相应H）；可用已有可靠实现或独立核验的简洁实现。不得按结果选择最好看的块长度。短样本/不足块数返回区间不足，不声称可靠。
4. 在完整时间轴上处理缺失值与连续区段；不得丢掉缺失日期后把相隔几周的数据当相邻日。抽样时成对重采相同日期的差与可用掩码，必要时输出不同缺失政策的预登记敏感性。分年/两年表是描述，不等于时间块置信区间。
5. 加回归：共同10只排序相同、另2只只在基线可评分，配对增量=0；无共同n时null；乱序/重复防护；已知20块的400个完全相关复制值不能按400个独立样本给不确定性；独立序列对照不被人为扩大。
6. 开发区均已看过，区间仅探索性；不能据几千次尝试中的最好区间宣称独立发现。真正晋升继续另验，不擅自用留出区。

## D. 事件层与门槛实验

1. 连续触发片段数与H收益窗口重叠组分开。按可用时点/预登记标签起止构建区间，明确包含端点规则、代表信号与传递并集。周末节假日不产生虚假间隔；中间一个非信号日也不自动解除20日区间重叠。
2. 结构事件 setup_id 与统计依赖 cluster_id 分开。B使用可恢复的 frozen_setup.setup_id；旧B0若只存状态而不可恢复，标 SOURCE_SETUP_ID_UNRECOVERABLE，不造ID。A/C/D使用明确signal_episode，不能把eligible/rejected当结构ID。
3. 共享聚类实现被stats与实际ablation入口共同调用，不能helper已修main仍自写相邻日计数。输出每条信号到每种H分组的映射；主题内计数与跨主题全局去重分别报告。组数不是N_eff。
4. 单独评价最终eligible信号的5/20/63日描述性前瞻结果、同日同轨参考差、尾部、覆盖、成员集中。它们与评分器IC分列；稀疏时保留不足。
5. `N_SCORE_FLOOR_PLUS_10PCT` 只改阈值，所以评分器IC不变应为对照通过，不能因此说门槛无用。门槛影响应看最终信号结果与机会数量。保持原定义74×1.1=81.4，除非另登记不同变体。
6. 当NEXT_DAY_CONFIRM晚于T+1开盘，基于T收盘的标签只叫描述性信号诊断。没有实际可参与时间对齐或raw/公司行动证据时，不输出组合收益、胜率、可交易超额。

## E. 邻近权重语义与真实主题结论

1. 旧 `N_M_PLUS_5PP` / `N_S_PLUS_5PP` 实际是未归一化分量+0.05再除1.05；先把旧结果注明 legacy_raw_weight_bump，不改历史值。
2. 真+5pp的目标：`new_f=old_f+0.05`，其他非零权重乘 `(1-new_f)/(1-old_f)`。超过合法范围拒绝；固定不可用G不复活。先登记新ID/版本，才评分。输入0.2659574468应变0.3159574468，而不是0.3009118541。
3. 第一遍先完成纠正后同能力逐因子消融；据这些结果，对每主题给至多3个有具体理由的邻近候选。已经执行的统一邻近先导无需伪装成主题专属发现。未经负责人确认，不无限扩新窗口/家族/宏观组合。
4. 研究卡不能只按baseline样本数输出51continue/45inherit。每主题×家族至少给：轨道/基线、可比变体、共同日期和成员数、配对IC差与块区间、分年方向、覆盖变化/集中度/缺失敏感性、最终信号表现、可解释的保留/继续检验/无增量证据/继承。
5. 明确区分 DATA_CAPABILITY_BLOCKED、CROSS_SECTION_THIN、NO_ROBUST_INCREMENT 和可继续候选。不能强求删减或赢家，也不能用模板替代数值证据。所有24主题都有卡，不只热门主题。

## F. 可验收交付与停止条件

- 当前head CI与新增真实仓库回归日志，注明哪些只在隔离夹具测试。
- 输入哈希、相关代码版本、run manifest、变体定义、实际入口验签记录。
- 每主题一份紧凑结果，至少一张24主题总览；完整配对表按主题/家族分片，每片尽量不超过1MB。不要再只给73万行一个JSON。原始授权数据继续私有；给可核对的schema、分片哈希、共同成员集合hash与必要受控样例。
- 旧与新结果差异只解释算法、数据能力和统计原因，不回写B0。
- 重评分、各轨/变体有效配置、日期组、行数与分组数量分开计数。
- 完成本轮同能力消融、有限已登记邻近修订和证据卡后暂停扩大搜索；不合并、不晋升、不擅自调低门槛、不开放留出。

目标是用现有数据回答“哪个因子在何种主题/家族里提供可重复的增量”，不是继续累加无可解释意义的运行次数。
