/goal
你正在 iroha1145/option-pro 仓库实施“美股收盘选股算法研究 v1”。随此提示附上完整 option_pro_us_eod_research_v1 目录。先逐一读完 README、01–05 规格和 config 注册表，再行动。不得只提取某个分数公式后忽略时点、数据、风险和样本外协议。

目标：把个股详情技术/价格结构证据纳入共享纯数据特征层，为现有 24 个主题逐一实现四种不同候选算法，并对稳健/均衡/进取及短/中/长逐一评估。综合选股另有四种二级筛选，不是板块榜单拼接。允许空结果。仅使用完整美股日线，盘中不为选股触发外部数据获取。

代码核对基线是 8b620d760f05020d2c1b51a3c3228a89fd9f3120。执行时先检查最新 main 与该基线差异，保留已有正确改动，不假定文件仍完全一致。当前代码已经部分复用 features/price_action/vol_price_match，也已在 scanner 剔除未收盘日线；不要重复实现或声称这些从来没有。

【不可越界】
只建立研究分支和可回滚的新算法版本，保持线上默认、旧生产排序、A0、突破雷达 T1、日股、期权和账户权限行为不变。没有获得测试结论前不得把新算法设为生产默认。不要修改用户资金、开仓、交易或付费数据订阅。不能把股票收益当作期权收益。

【阶段 0：审计与冻结】
核对 sectors.py、strength/features.py、scoring.py、scanner.py、price_action.py、technical/base_structure.py、chart_analysis.py、strength 快照 API 和 worker 调度。列出可直接复用、必须重构、仅显示不可直接打分的部分。用真实证券元数据核验美股场所，不再以代码含不含 '.' 判断美股。记录同股跨主题、ETF 子资产、OTC/外市场和历史分类缺口。
建立 research 分支；输出 audit.md、data_audit.json、config_hash、source_sha、trial_ledger。现有 24 项任何一项都不能静默略过；无法回测也必须有原因和状态。

【阶段 1：确定性共享特征】
纯函数输入为历史数据、明确的 as_of、交易日历和版本化分类，不调用 now、不联网。计算 8 因子 T/M/S/B/P/V/R/G，严格按 02 规格区分复权、原始成交额、总回报和常规时段。共享给详情和筛选，不调用渲染层分数。记录 pivot_at/confirmed_at、冻结 setup_id、阻力带、首次突破、确认和失效。
专门测试“在 T 之后追加未来数据，不改变 T 输出”。所有回看陷阱、形态、多窗平台、标尺和百分位都要检查未来函数。未确认不是中性分；没有观察到形态与数据缺失必须区分。相同母池中切换界面过滤条件不能改变一只股票的基础因子。

【阶段 2：全板块候选实现】
读取机器配置，解析 24×4×3×3=864 个主配置。权重以 JSON 显式配置为准；规范有歧义时先写明解释和新增测试，不得偷偷凭收益调参。每个板块必须验证四家族 A/B/C/D 的门槛、分数、子组、风险档和资金容量差异。ETF 按五个子资产额外给出 180 个诊断切片，主表 ETF 36 行不可混合金债并声称股票 alpha。
reference/registry.py 只是共享评分和容量的参考，不是完整指标引擎、交易模拟器或生产补丁。你需要实现未提供的原始特征引擎、数据适配、批处理和回测；不得把单元测试结果写成收益验证。

【阶段 3：十年以上回测】
先核实授权数据。尽量覆盖 2010 至最新完整交易日，至少十年并加预热。分别运行 U_current 与 U_PIT；退市、公司行动、行业变迁、代码复用、IPO 缺样本不能忽略。十年前没有某新主题标签，不能人为构造“当时 AI 股票池”。不足处标记 DATA_INSUFFICIENT/INSUFFICIENT_PIT_HISTORY；可做母组实验但不得替代主题检验。
严格采用 04 协议：训练/校准/滚动测试，成熟标签与 purge；T 收盘后信号最早 T+1 开盘，持有 5/20/63 时段并固定退出；手续费滑点与 2×/4×压力；有限资金、持仓去重与现金记账；旧排序/A0/12–1/均线/同池等权及市场/行业对照。禁止同收盘信号同收盘成交，禁止看到下一开盘价后仍选择按该价成交。
只在此前年度数据内挑选第一层模型并训练第二层，不使用当前测试年度的结果。已看过的 2024–2025 不得称为全新留出。样本不足的高胜率不算合格。所有尝试包括失败、零交易和临时变体都进入 ledger。

【阶段 4：独立综合层】
在相同输入、执行和切分下评估 M1/M2/M3/M4 各三种风险档；分数校准来自历史折外数据，多主题证券去重，拒绝信号不被重新捞回。timeframe=all 用 20 日主排名、63 日趋势约束、5 日时机否决。不得变回简单的 .5mid+.5long。

【阶段 5：EOD shadow 接入与性能】
收盘后 batch 获取一次合资格证券日线及基准，落地版本化原始数据，计算一次每日特征矩阵，复用为各算法视图。不因 864 种配置下载 864 次行情。日历使用 America/New_York，正确处理夏令时、半日交易和假期。收盘后的延迟窗口仅为调度，不是数据最终可用的证据；provider 完整性和成交量时段必须核验。
盘中选股 API、换板块、换 TopK、切风险档、按钮点击只读取/计算本地已完成日快照；不得调用外部行情、期权链或新闻接口。缺缓存可本地组合已有特征；不能前台回落实时扫描。数据缺失时返回旧的明确标记快照或不可用，不要伪造今天日期。详情页和其它实时业务不受影响。
给出网络调用计数、重复刷新测试、并发和移动端回归结果、cache-key 与原子发布方案。后台失败必须保留上一个完整快照。不要为了大回测占满生产服务器，使用研究环境和持久缓存。

【测试清单】
必须覆盖：复权/分红/拆股；时间与半日市；数据源修订/迟到；未确认 pivot；多窗平台冻结；假突破事件确认；信号可重复重算；无未来数据；缺失门槛；无成交量/停牌；去重；零结果；极端波动；ETF 分轨；风险档和资金档；模拟资金守恒；成本越大净收益不增；同日多笔不重复使用资金；短中长末端未成熟标签；旧接口和默认算法兼容。

【完成条件与回传】
先完成第一轮全部候选比较，不无限循环调优到回测漂亮。若 data 权限不足，完成可完成的实现/工程测试并提交准确阻塞清单；不能虚报测试覆盖。初筛后先停下等用户和本研究方案制定者审阅，不擅自进行大规模参数优化。
请生成一个压缩包，包含：
1. audit.md、data_audit.json、source/config/data hashes 和完整 trial_ledger。
2. all_experiments.csv 或 parquet：864 行主结果，失败也有行；ETF 180 切片；composite 12 行；全部资金成本子表。
3. annual_oos.csv、regime_oos.csv、subgroup_oos.csv、capital_cost_scenarios.csv。
4. signals.parquet、trades.parquet、daily_equity.parquet、rejections.parquet，附 schema；体积大时给分区索引和可核验样本。
5. 按 24 个板块分别写 sector_reports/<sector>.md，逐风险档周期说明四候选对照、样本量、暂留/淘汰/无赢家理由。
6. templates/return_summary.json 的完整填充值、工程测试日志、性能/外部请求计数和代码 diff。

用状态 REGISTERED_NOT_RUN / DATA_INSUFFICIENT / INSUFFICIENT_PIT_HISTORY / STATISTICALLY_THIN / FAILED / COMPLETED_UNPROMOTED / SHADOW_ONLY 准确区分。只有真实跑过的市场试验才允许写收益数字；没有任何版本在本提示词阶段被认定为赢家。
