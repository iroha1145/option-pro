# Research data directories

`local/` and `cache/` hold authorized exports or downloaded diagnostic bars.
They are gitignored. Path names are not evidence that market history exists.

Use `RESEARCH_EOD_LOCAL_DATA` to point at an authorized tree.
