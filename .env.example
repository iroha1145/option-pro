# Personal Edition legacy compatibility layer.
# New machine settings belong in machine.env and secrets belong in secrets.env.
# Keep this file so one migration release can read old deployments before the
# canonical files override them. New installations do not add settings here.
